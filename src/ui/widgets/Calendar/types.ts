export interface RoundedSide {
	leftTop?: boolean
	rightTop?: boolean
	rightBottom?: boolean
	leftBottom?: boolean
}

export enum DaysDisplayVariants {
	DEFAULT = 'default',
	THREE_DAYS = 'days',
	WEEK = 'week',
	MONTH = 'month'
}
